# 에러 수정 규칙

에러를 수정할 때는 수정에 필요한 코드만 최소한으로 읽는다.

- 에러 메시지와 스택 트레이스를 먼저 분석해 원인 파일과 라인을 특정한다.
- 해당 파일의 관련 부분만 읽는다. 전체 파일을 불필요하게 읽지 않는다.
- 수정과 무관한 파일(설정 파일, 다른 모듈 등)은 읽지 않는다.
- 연관 파일이 필요한 경우 해당 함수/타입 정의 부분만 Grep으로 찾아 읽는다.
- 수정 범위를 벗어난 리팩토링이나 개선은 하지 않는다.
