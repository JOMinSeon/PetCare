# 구독 결제 페이지 수정 계획

> 작성일: 2026-03-24
> 대상 브랜치: main

---

## 1. 현재 구조 파악

### 관련 파일 목록

| 역할 | 파일 경로 |
|------|-----------|
| 요금제 소개 페이지 | `app/(main)/pricing/page.tsx` |
| 요금제 선택 UI | `app/(main)/pricing/PricingClient.tsx` |
| **카드 등록 & 결제 페이지** | `app/(main)/subscribe/page.tsx` ← 핵심 |
| 구독 관리 페이지 | `app/(main)/subscription/page.tsx` |
| 결제 완료 페이지 | `app/(main)/billing/complete/page.tsx` |
| 빌링키 저장 & 첫 결제 API | `app/api/portone/billing-key/route.ts` |
| 정기 결제 실행 API | `app/api/portone/charge/route.ts` |
| 구독 취소 API | `app/api/portone/cancel/route.ts` |
| 재결제 API | `app/api/portone/retry/route.ts` |
| 카드 변경 API | `app/api/portone/change-card/route.ts` |
| PortOne 웹훅 | `app/api/portone/webhook/route.ts` |
| Cron 정기 결제 | `app/api/billing/cron/route.ts` |
| PortOne 클라이언트 | `lib/portone.ts` |
| 플랜 정의 & 가격 | `lib/plans.ts` |
| 구독 취소 Server Action | `app/actions/subscription.ts` |

### 현재 결제 흐름

```
1. /pricing → 플랜 선택 (월간/연간 토글)
       ↓
2. /subscribe?planId=premium&cycle=monthly
   - 이메일 (읽기 전용)
   - 휴대폰 번호 입력 (DB에 없으면 필수 입력)
   - "KG이니시스 카드 등록 및 결제" 버튼 클릭
       ↓
3. PortOne.requestIssueBillingKey() 호출 → 카드사 팝업
       ↓
4. POST /api/portone/billing-key
   - 빌링키 소유권 검증
   - 첫 결제 실행 (payWithBillingKey)
   - 금액 재검증
   - DB 업데이트 (profiles, payment_history)
       ↓
5. /settings?payment=success 리다이렉트
```

### DB 컬럼 (profiles 테이블)

```
subscription_plan    : 'free' | 'premium' | 'clinic'
subscription_status  : 'active' | 'inactive' | 'payment_failed'
billing_cycle        : 'monthly' | 'yearly'
nicepay_bid          : 빌링키 저장값
plan_started_at      : 구독 시작일
next_billing_at      : 다음 결제일
```

### 플랜 정의 (lib/plans.ts)

| 플랜 | 월간 | 연간(월 환산) | 연간 일괄 |
|------|------|--------------|----------|
| free | ₩0 | - | - |
| premium | ₩14,900 | ₩12,400 | ₩149,000 |
| clinic | ₩49,000 | ₩40,800 | ₩490,000 |

---

## 2. 수정 항목

> 아래 항목에 체크하며 진행

### UI / 디자인 수정

- [ ]
- [ ]
- [ ]

### 기능 수정

- [ ]
- [ ]
- [ ]

### API / 백엔드 수정

- [ ]
- [ ]

### 플랜 / 가격 수정

- [ ]
- [ ]

---

## 3. 수정 시 주의사항

### 결제 금액 검증 이중화
`/api/portone/billing-key/route.ts`에서 PortOne API로 실제 결제 금액을 재조회해 검증함.
→ 클라이언트에서 금액을 변경해도 서버에서 차단되므로, **금액 변경 시 반드시 서버 코드도 같이 수정해야 함.**

### 빌링키 소유권 검증
결제 API는 PortOne `getBillingKey()`로 `customerId`를 검증함.
→ `customerId = userId.replace(/-/g, '')` 형식으로 고정되어 있음.

### 카드 변경 vs 신규 구독
`/subscribe` 페이지에서 `?changeCard=true` 파라미터로 분기됨.
- 카드 변경: `/api/portone/change-card` 호출 후 `/subscription?card=changed`
- 신규 구독: `/api/portone/billing-key` 호출 후 `/settings?payment=success`

### 웹훅 중복 처리 방지
`webhook_events` 테이블의 `UNIQUE(event_id)`로 중복 방지됨.
→ 웹훅 이벤트 타입 추가 시 `app/api/portone/webhook/route.ts` 수정 필요.

### Cron 정기 결제
`vercel.json`의 `crons` 설정에서 스케줄 변경 가능.
현재: `next_billing_at <= now()` AND `subscription_status = 'active'`인 사용자만 처리.

---

## 4. 환경 변수 목록

```bash
# PortOne v2
NEXT_PUBLIC_PORTONE_STORE_ID=
NEXT_PUBLIC_PORTONE_CHANNEL_KEY=
PORTONE_API_SECRET=
PORTONE_WEBHOOK_SECRET=

# Cron
CRON_SECRET=

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
```

---

## 5. 테스트 체크리스트

- [ ] 신규 구독 (월간) 정상 결제
- [ ] 신규 구독 (연간) 정상 결제
- [ ] 결제 실패 시 에러 메시지 표시
- [ ] 카드 변경 후 정상 작동
- [ ] 구독 취소 후 상태 변경 확인
- [ ] 재결제 버튼 동작 확인
- [ ] 웹훅 수신 후 DB 상태 동기화 확인
- [ ] Cron 정기 결제 시뮬레이션

---

## 6. 참고

- PortOne v2 공식 문서: https://developers.portone.io/docs/ko/v2-payment/readme
- KG이니시스 빌링키 방식 연동 가이드: PortOne 대시보드 내 채널 설정 참조
